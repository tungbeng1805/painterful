<div class="stats-wrapper">
  <div class="stat-box">
    <div class="stat-icon">
        {{ _block.settings.svg_1 }}
    </div>
    <div>
        <div class="stat-number">{{ _block.settings.star_rating_number_1 }}</div>
        <div class="stat-label">{{ _block.settings.star_rating_label_1 }}</div>
    </div>
  </div>

  <div class="stat-box">
    <div class="stat-icon">
        {{ _block.settings.svg_2 }}
    </div>
    <div>
        <div class="stat-number">{{ _block.settings.star_rating_number_2 }}</div>
        <div class="stat-label">{{ _block.settings.star_rating_label_2 }}</div>
    </div>
  </div>

  <div class="stat-box">
    <div class="stat-icon">
        {{ _block.settings.svg_3 }}
    </div>
    <div>
        <div class="stat-number">{{ _block.settings.star_rating_number_3 }}</div>
        <div class="stat-label">{{ _block.settings.star_rating_label_3 }}</div>
    </div>
  </div>
</div>
<style>
.stats-wrapper {
  display: flex;
  gap: 24px;
  justify-content: center;
  flex-wrap: wrap;
  margin-top: 20px
}

.stat-box {
  background: #F1EEEA;
  border-radius: 16px;
  padding: 24px 28px;
  text-align: center;
  width: 30%;
}

.stat-icon {
  font-size: 26px;
  color: #d56b2d;
  margin-bottom: 10px;
}

.stat-icon svg {
    margin-inline: auto;
}

.stat-number {
  font-size: 18px;
  font-weight: 700;
  color: #000;
  margin-bottom: 4px;
}

.stat-label {
  font-size: 12px;
  color: #8b6f5a;
}
@media(max-width: 575px){
    .stat-box {
        padding: 5px;
    }
    .stat-icon svg {
        width: 16px;
    }
    .stat-number {
        font-size: 12px;
    }
    .stat-label {
        display: none;
    }
    .stats-wrapper {
        gap: 5px;
    }
    .stat-icon {
        margin-bottom: 0px;
    }
}
</style>
